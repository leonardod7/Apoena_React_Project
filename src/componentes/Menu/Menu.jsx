import {Link} from "react-router-dom";

const Menu = ( ) => (
    <nav>
        <Link to="/"> Home </Link>
        <Link to="/teste"> Teste </Link>
        
    </nav>
);

export default Menu;
